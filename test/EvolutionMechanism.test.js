const EvolutionMechanism = require('../server/core/EvolutionMechanism');

describe('EvolutionMechanism', () => {
  test('should be defined', () => {
    expect(EvolutionMechanism).toBeDefined();
  });
});
