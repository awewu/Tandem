const ConditionalFieldEngine = require('../server/core/ConditionalFieldEngine');

describe('ConditionalFieldEngine', () => {
  test('should be defined', () => {
    expect(ConditionalFieldEngine).toBeDefined();
  });
});
