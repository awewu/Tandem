const PerformanceMonitor = require('../server/core/PerformanceMonitor');

describe('PerformanceMonitor', () => {
  test('should be defined', () => {
    expect(PerformanceMonitor).toBeDefined();
  });
});
