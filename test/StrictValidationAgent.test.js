const StrictValidationAgent = require('../server/core/StrictValidationAgent');

describe('StrictValidationAgent', () => {
  test('should be defined', () => {
    expect(StrictValidationAgent).toBeDefined();
  });
});
