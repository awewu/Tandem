const AIDesignAssistant = require('../server/core/AIDesignAssistant');

describe('AIDesignAssistant', () => {
  test('should be defined', () => {
    expect(AIDesignAssistant).toBeDefined();
  });
});
