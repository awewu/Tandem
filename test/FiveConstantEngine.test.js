const FiveConstantEngine = require('../server/core/FiveConstantEngine');

describe('FiveConstantEngine', () => {
  test('should be defined', () => {
    expect(FiveConstantEngine).toBeDefined();
  });
});
