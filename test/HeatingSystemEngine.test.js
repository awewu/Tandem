const HeatingSystemEngine = require('../server/core/HeatingSystemEngine');

describe('HeatingSystemEngine', () => {
  test('should be defined', () => {
    expect(HeatingSystemEngine).toBeDefined();
  });
});
