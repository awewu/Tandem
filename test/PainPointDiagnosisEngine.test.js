const PainPointDiagnosisEngine = require('../server/core/PainPointDiagnosisEngine');

describe('PainPointDiagnosisEngine', () => {
  test('should be defined', () => {
    expect(PainPointDiagnosisEngine).toBeDefined();
  });
});
