const EnterpriseValidationEngine = require('../server/core/EnterpriseValidationEngine');

describe('EnterpriseValidationEngine', () => {
  test('should be defined', () => {
    expect(EnterpriseValidationEngine).toBeDefined();
  });
});
