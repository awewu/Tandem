const LoadCalculationEngine = require('../server/core/LoadCalculationEngine');

describe('LoadCalculationEngine', () => {
  test('should be defined', () => {
    expect(LoadCalculationEngine).toBeDefined();
  });
});
