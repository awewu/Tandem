const ChannelManagementEngine = require('../server/core/ChannelManagementEngine');

describe('ChannelManagementEngine', () => {
  test('should be defined', () => {
    expect(ChannelManagementEngine).toBeDefined();
  });
});
