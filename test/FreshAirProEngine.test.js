const FreshAirProEngine = require('../server/core/FreshAirProEngine');

describe('FreshAirProEngine', () => {
  test('should be defined', () => {
    expect(FreshAirProEngine).toBeDefined();
  });
});
