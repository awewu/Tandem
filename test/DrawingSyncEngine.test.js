const DrawingSyncEngine = require('../server/core/DrawingSyncEngine');

describe('DrawingSyncEngine', () => {
  test('should be defined', () => {
    expect(DrawingSyncEngine).toBeDefined();
  });
});
