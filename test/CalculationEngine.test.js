const CalculationEngine = require('../server/core/CalculationEngine');

describe('CalculationEngine', () => {
  test('should be defined', () => {
    expect(CalculationEngine).toBeDefined();
  });
});
