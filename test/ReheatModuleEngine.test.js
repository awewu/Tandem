const ReheatModuleEngine = require('../server/core/ReheatModuleEngine');

describe('ReheatModuleEngine', () => {
  test('should be defined', () => {
    expect(ReheatModuleEngine).toBeDefined();
  });
});
