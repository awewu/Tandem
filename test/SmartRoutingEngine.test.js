const SmartRoutingEngine = require('../server/core/SmartRoutingEngine');

describe('SmartRoutingEngine', () => {
  test('should be defined', () => {
    expect(SmartRoutingEngine).toBeDefined();
  });
});
