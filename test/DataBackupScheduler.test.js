const DataBackupScheduler = require('../server/core/DataBackupScheduler');

describe('DataBackupScheduler', () => {
  test('should be defined', () => {
    expect(DataBackupScheduler).toBeDefined();
  });
});
