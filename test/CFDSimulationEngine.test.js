const CFDSimulationEngine = require('../server/core/CFDSimulationEngine');

describe('CFDSimulationEngine', () => {
  test('should be defined', () => {
    expect(CFDSimulationEngine).toBeDefined();
  });
});
