{
  "name": "@rhautt/engines",
  "version": "0.0.0",
  "private": true,
  "description": "Shared calculation-engine surface (Strangler chokepoint). NestJS consumes engines via @rhautt/engines instead of deep relative requires into server/core.",
  "main": "src/index.js",
  "types": "src/index.d.ts"
}
