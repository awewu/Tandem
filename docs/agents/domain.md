# Domain Docs

This repository uses a single-context domain layout.

## Before exploring

- Read the root `CONTEXT.md` when it exists.
- Read relevant decisions under `docs/adr/` when that directory exists.
- If either location is absent, continue silently.

Use domain terms defined by the project documentation and explicitly flag proposals that conflict with an existing ADR.
