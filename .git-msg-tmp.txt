feat(im+okr): IM 按部门一键建群 + /1on1 主管员工对话模块 (P1 双收)

User: 继续开发. IM 和 OKR 的 P1 各收 1 个.

==== IM P1: 按组织架构一键建群 (HR seed) ====

lib/im/service.ts (+80 行):
  DepartmentSpec / SeedResult 类型
  seedDepartmentChannels(specs, operatorId) - 幂等
  - 已存在 departmentId + autoCreated=true 的跳过
  - 空成员 / 创建失败 分别记录 skipped.reason

app/api/im/channels/seed-from-org/route.ts (NEW, 46 行):
  POST 一键触发, 严格 body 校验

components/im/seed-from-org-dialog.tsx (NEW, 255 行):
  - 展平 departments + ministries 为可勾选行
  - 默认全选 + 成员数实时统计
  - 执行后显示 created/skipped 分组结果
  - 无组织数据时提示去 /organization 配置

/im 顶栏加 Sparkles 按钮 (amber 色) → 开对话框.

==== OKR P1: /1on1 主管员工周期对话 ====

lib/store.ts (+130 行):
  OneOnOneCadence (weekly/biweekly/monthly/adhoc)
  OneOnOneStatus (scheduled/completed/cancelled/no-show)
  OneOnOneActionItem + OneOnOneMeeting 结构:
    managerId + reportId + cadence + scheduledAt
    agendaManager + agendaReport (双方各写议程)
    noteProgress + noteBlockers + noteNextSteps (三段式)
    linkedKrIds (连 OKR, Tandem 差异化)
    actionItems[] 带 assignee + dueDate + done
    moodScore 1-5 (仅主管可见, §13 隐私)
    privateManagerNote (仅主管可见)

  useOneOnOneStore + CRUD: addMeeting / updateMeeting / deleteMeeting
    / addActionItem / toggleActionItem / removeActionItem

app/1on1/page.tsx (NEW, 590 行):
  双栏: 左会议列表 (即将/已完成/其他 3 段), 右详情
  详情 5 大区:
    1. Header - 参与人 + 状态 + 开始/完成/取消/删 按钮
    2. 议程 - 主管和员工各自的 talking points (双 textarea)
    3. 关联 KR - 从 useOKRStore 选 + 多选 (避免空聊)
    4. 三段式 notes - 进展/障碍/下一步 (emerald/rose/blue 色)
    5. Action Items - 带 assignee select + dueDate + 打勾 + 删除
  主管私密区 (amber 卡片, 仅 isManager 可见):
    - 1-5 干劲评分
    - 私密笔记
    - §13 数据尊严声明

components/sidebar.tsx (+1 行):
  事半模块加 '1on1 对话' → /1on1

==== Tandem vs Tita 1on1 差异化 ====

Tita 1on1:
  - 议程 / notes / action items
  - 无 KR 强绑定
  - 心情评分公开

Tandem 1on1:
  + 关联 KR 必填提示 (反空聊, 连 OKR 避免形式主义)
  + moodScore + privateManagerNote 严格主管私有 (§13 尊严)
  + M2 可 actionItem -> Initiative 转化
  + 纯 client zustand, 单用户 demo 模式

==== Verified ====

npx tsc --noEmit → 0 errors
GET /im → 200 (+1.6KB, 新加 seed 按钮)
GET /1on1 → 200 (25KB 新页)
pre-commit hook 通过

==== Coverage 更新 ====

IM 70% → 75% (seed 干掉 P1 一项, 剩部门群可用了)
OKR 95% → 97% (1on1 是 P1 5 天最大项, MVP 落地)

剩 P1:
  IM: 多端同步 (20 天 · 涉及后端重构, V2)
  OKR: 360 评估 / 部门 dashboard / 日历 / 季度模板

==== 4 铁律 checkin ====

#1 不删 - 0 删除
#2 累积演进 - 新 tab/新 route, 不动既有
#3 tsc 0 errors
#4 增量 - 2 服务函数 + 1 API + 2 组件 + 1 页面 + 1 sidebar 行.
    零改 existing storage / messaging / okr core.
