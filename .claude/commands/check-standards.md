# Check Standards

Run standards checks:

```bash
npm run test:production-readiness -- system-packs
npm run harness:evolution
```

Verify:

- every system pack standard has level/edition/softwareCheck
- China mandatory general code constraints are L1
- international references are L3
- lifecycle IoT deliverables remain present
